--!strict
-- This fixture is intentionally not uploaded to GitHub Code Scanning. It keeps
-- in-source suppression coverage in local/golden tests without creating alerts.
-- rbx-heal: ignore RBX-TASK-001 -- suppression fixture requires legacy scheduler
wait(0)
